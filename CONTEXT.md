# instrumentationx 上下文

本文件定义 `instrumentationx` 与其使用方共享的核心词汇。它只记录领域含义与能力边界，
不记录具体实现、API 签名、存储或部署决定。

## 角色与边界

**注入点**：消费方在重试或熔断状态迁移发生时调用的同步记录点；它只描述「何时应当记录什么」，
不规定记录落到哪里。
_Avoid_: 观测后端（后端指具体的落地设施与供应链，本 crate 不提供任何后端）

**契约 crate**：只声明共享词汇与 trait、不携带实现的微 crate；它不引入依赖，也不允许实现细节
从主工程泄漏进来。
_Avoid_: 公共库（公共库通常还包含工具实现，会掩盖「只有契约」这一边界）

**消费方**：在热路径上调用注入点的一方（例如 `resiliencx`）；它只持有 trait object，
不感知落地方式。
_Avoid_: 调用者（过于宽泛，不能表达「只依赖契约、不依赖实现」）

**实现方**：把注入事件落到具体观测设施的一方（例如 `observex`）；它承担清理、限长与非阻塞义务。
_Avoid_: 适配器、plugin（前者易与平台适配器混淆，后者暗示动态加载）

## 语义与生命周期

**op（操作名）**：调用方给出的、稳定的分层操作名，用于把事件归到某个业务或基础设施操作。
_Avoid_: 请求 ID、订单号（高基数标识会放大 metrics 后端的成本）

**attempt（重试序次）**：同一次重试序列中的第几次尝试；起点（`0` 或 `1`）由调用方约定，
实现方不得假设。
_Avoid_: 重试次数（「次数」暗示从 0 起的确定总量，不能表达起点的约定性）

**三类事件**：重试、熔断打开、熔断关闭；它们是记录面的全部集合，新增方法属于破坏性变更。
_Avoid_: 指标、span（那两个词是落地形态，不是契约词汇）

**对象安全**：trait 可以退化为 `dyn Instrumentation`，不带泛型参数、关联类型或 `Self: Sized`
约束；这使注入与共享不依赖具体类型。
_Avoid_: 泛型接口（泛型 trait 无法直接作为 trait object 注入）

**Send + Sync 约束**：实现必须可跨线程共享，因为消费方可能在任意工作线程上触发事件。
_Avoid_: 线程安全（过于笼统，不能表达 `Send + Sync` 这一 supertrait 约束）

## 义务与失败

**清理与限长**：实现方在写入观测后端前移除控制字符并限制 `op` 长度，抵御日志注入与存储放大；
契约本身不做这项处理。
_Avoid_: 输入校验（校验暗示可以拒绝，而这里是就地规整而非拒绝）

**非阻塞义务**：三类方法都不应阻塞调用方；慢后端由实现方自行做有界缓冲或异步落地。
_Avoid_: 异步接口（本 crate 不定义任何 `async` 方法）

**不 panic 义务**：`record_*` 不得把实现方内部错误向消费方传播，只能内化为计数、丢弃或降级。
_Avoid_: 错误处理（记录点不返回 `Result`，没有错误通道可走）
